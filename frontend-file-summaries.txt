{
  "frontend/src/App.tsx": "Root app component that renders the RankingsPage as the main view.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching raw ranking data, searching players, fetching all players, and a `useAddPlayer` mutation; the fallback object in `useRawRankingData` now uses the updated keys `spearMace` and `diamondSmpNethopSpear` instead of the old `ltms` and `pot`.",
  "frontend/src/data/mockData.ts": "Exports an empty `mockPlayerData` array, a `CategoryKey` union type for the 10 game categories (with `spearMace` replacing `ltms` and `diamondSmpNethopSpear` replacing `pot`), and an empty `MOCK_DATA` record keyed by `CategoryKey`.",
  "frontend/src/components/TierBadge.tsx": "Compact circular tier badge component with SVG category icons and color-coded tier labels; category config keys updated from `ltms`/`pot` to `spearMace`/`diamondSmpNethopSpear` with appropriate icons.",
  "frontend/src/components/PlayerRow.tsx": "Individual player row with gold/silver/bronze rank backgrounds, crafatar.com avatar, username, title/points, region pill badge, and tier badges.",
  "frontend/src/components/RankingsTable.tsx": "Rankings table with header row, loading skeletons, empty state, and filtered player rows based on search query.",
  "frontend/src/components/CategoryTabs.tsx": "Horizontal scrollable tab bar with 10 category tabs; `LTMs` renamed to `Spear Mace` (key `spearMace`) and `Pot` renamed to `Diamond SMP Nethop Spear` (key `diamondSmpNethopSpear`), with matching icon component renames.",
  "frontend/src/components/SearchInput.tsx": "Search input with magnifier icon, clear button, and keyboard shortcut hint styled for the dark MC theme.",
  "frontend/src/pages/RankingsPage.tsx": "Main rankings page that now fetches live player data from the backend via `useGetAllPlayers`, filters by active category, sorts by points, and passes ranked players to RankingsTable; AdminControls no longer receives an `onAddPlayer` prop.",
  "frontend/src/index.css": "Global dark Minecraft-inspired theme with OKLCH color tokens, Press Start 2P font import, pixelated image utility, rank gradient utilities, and scrollbar-hide helper.",
  "frontend/tailwind.config.js": "Extended Tailwind config with mc.* color palette (dark navy/charcoal backgrounds, gold accents, tier badge colors), pixel font family, and MC-specific shadow tokens.",
  "frontend/src/components/PasscodeModal.tsx": "Modal dialog that prompts the user to enter the admin passcode (65515616151); shows an error on incorrect entry and calls onSuccess when the correct code is submitted.",
  "frontend/src/components/AdminControls.tsx": "Admin controls component with Add Player and Remove Player buttons; manages passcode unlock state via PasscodeModal, and opens the new AddPlayerModal directly after unlock (or immediately if already unlocked). The `onAddPlayer` prop has been removed in favour of internal modal management.",
  "frontend/src/main.tsx": "App entry point that wraps the app with QueryClientProvider only; InternetIdentityProvider has been removed.",
  "frontend/src/components/AddPlayerModal.tsx": "Dark-themed modal dialog for adding a new player with controlled inputs; tier badge game mode labels updated so `LTMs` → `Spear Mace` and `Pot` → `Diamond SMP Nethop Spear` with corresponding key changes."
}